
$(document).ready(function () {

	var spiders = document.querySelector("#spiders");
	spiders.style.display = "inline-block";

	// /* HERO ============================================================ */
	// // to detect if this is a mobile
	// var phone = false,
	// 	platform = navigator.platform;
	// if (platform === 'iPad' || platform === 'iPhone' || platform === 'iPod' || platform === 'Android' || platform === 'BlackBerry' || platform === 'Linux' || platform === 'Linux i686' || platform === 'Linux i686 X11' || platform === 'Linux armv7l' || platform === 'Linux x86_64 X11' || platform === 'null' || platform === 'undefined') {
	// 	phone = true;
	// }
	// if (phone === false) {
	// 	// for greensocks animaton in hero
	// 	var spiders = document.querySelector("#spiders");
	// 		spiders.style.display = "inline-block";
	// }
	// if (phone === true) {
	// 	// for greensocks animaton in hero
	// 	var spiders = document.querySelector("#spiders");
	// 		spiders.style.display = "none";
	// }

});
